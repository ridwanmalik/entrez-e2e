# ─── Android device ────────────────────────────────────────────────────────────
# Run `adb devices` to get UDID for a real device, or `emulator -list-avds` for emulators
ANDROID_DEVICE_NAME=My Android Device
ANDROID_UDID=
ANDROID_PLATFORM_VERSION=13.0

# ─── App under test ────────────────────────────────────────────────────────────
APP_PATH=./apps/app-debug.apk
APP_PACKAGE=com.yourcompany.fooddelivery
APP_ACTIVITY=.MainActivity

# ─── Appium server ─────────────────────────────────────────────────────────────
APPIUM_HOST=localhost
APPIUM_PORT=4723

# ─── Timeouts (ms) ─────────────────────────────────────────────────────────────
EXPLICIT_WAIT_MS=30000
